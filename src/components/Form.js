import React from 'react';

const Form = (props) => {

    return (
        <form className="regex-input" >
          <input type="text" name="input" id="input" />  
        </form>
    )
};

export default Form;